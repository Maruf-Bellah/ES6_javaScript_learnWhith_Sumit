// ternary operator 

var age = 1;

// var type ;

// if(age >= 18){
//     type ='adult';
// }else{
//     type = 'child';
// };

// console.log(type);

// uper code k sohoj vabe lekha jay ternary operatorer maddhome 

let type = age >= 18 ? 'Adult' : 'Child';
// console.log(type);

// nested ternary operator 

var types = age >= 18 ? 'Adult' : age < 10 ? 'child' : 'young';


var a = 5;

var access = a === 5 ;
console.log(access);